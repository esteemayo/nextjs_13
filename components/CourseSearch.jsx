import { useState } from 'react';

const CourseSearch = () => {
  const handleSubmit = (e) => {
    e.preventDefault();
  };

  return (
    <div>CourseSearch</div>
  );
}

export default CourseSearch;
